# Freemium – Quick Start

## Local Desktop / Docker
```bash
# 1. Clone
$ git clone https://github.com/jhastrovio/Freemium.git && cd Freemium

# 2. Build & run
$ docker compose up --build  # or `npm i && npm run dev`
```