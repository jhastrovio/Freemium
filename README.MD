# Freenium – Quick Start

## Local Desktop / Docker
```bash
# 1. Clone
$ git clone https://github.com/jhastrovio/Freenium.git && cd Freenium

# 2. Build & run
$ docker compose up --build  # or `npm i && npm run dev`